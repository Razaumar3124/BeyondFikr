import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        custom: {
            theme1: "rgb(48,107,115)",
            theme2: "rgb(106,184,204)",
        }
    }
})

export default theme